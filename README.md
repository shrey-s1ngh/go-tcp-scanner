This file contains a basic usage of net package in go to scan ports from 1-1024.
It utilizes the go's concurrency with 100 threads and channel to store the result.
we are scanning the nmap's site here which is : "canme.nmap.org"
